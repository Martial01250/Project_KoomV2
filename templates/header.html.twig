

<nav>

    <a href="{{ path('app_home') }}" class="nav-icon" aria-label="visit homepage" aria-current="page">
        <img class="" src="{{ asset('build/img/Logo_Koom_black.png') }}" alt="logo koom">
    </a>

    <div class="main-navlinks">
        <button class="hamburger" type="button" aria-label="Toggle navigation" aria-expanded="false">
            <span></span>
            <span></span>
            <span></span>
        </button>
        <div class="navlinks-container">
            <a href="{{ path('app_home') }}" aria-current="page">Accueil</a>

            <a href="#">Projets</a>
            {% if not app.user %}
            <a href="{{ path('app_login')}}">Se connecter</a>
            <a href="{{ path('app_register') }}">S'inscrire</a>
            {% endif %}

            {% if app.user %}
                <a href="{{ path('app_logout')}}">Se déconnecter</a>
            {% endif %}
        </div>
    </div>

    <div class="nav-authentication">
        <a href="{{ path('app_login') }}" class="sign-user" aria-label="sign in page">
            <img src="{{ asset('build/img/LogoUser.png') }}" alt="user-icon">
        </a>
    </div>

</nav>