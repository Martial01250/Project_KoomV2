{% block ProjectCard %}

            <div class="col-12 col-md-6 col-lg-3">
                <div class="card">
                    <img src="{{ asset('build/img/VectorHokey.06019c3a.png') }}" class="card-img-top p-3" alt="image project">
                    <div class="card-body">
                        <h5 class="card-title">{{ project.name }}</h5>
                        <p class="card-text">{{ project.shortDescription }}</p>
                        <a href="{{ path('app_project_show', {'id': project.id}) }}" class="btn btn-primary">en apprendre plus </a>
                    </div>
                </div>
            </div>

{% endblock %}