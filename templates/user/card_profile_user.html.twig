<div class="card card_bgc">
    <div class="card-header d-flex">
        <div class="col-5">
            <img class="img-thumbnail" src="{{ asset('build/img/test_profil.png') }}" alt="test image">
        </div>
        <div class="col-7 text-center">
            <h4 class="mt-4">{{ app.user.firstName }} {{ app.user.lastname }}</h4>
            <p class="mt-4">{{ app.user.email }}</p>
        </div>
    </div>

    <div class="card-footer">
        <ul>
            <table class="table">
                <thead>
                <tr>
                    <th scope="col">Compétences</th>
                    <th scope="col"></th>
                    <th scope="col">Technologie</th>
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td>

                    </td>
                </tr>
                </tbody>
            </table>
        </ul>
    </div>
</div>